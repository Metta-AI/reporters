name: "Build Cogs vs Clips Summarizer Image"

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/build-cogs-vs-clips-summarizer-image.yml"
      - "reporters/cogs_vs_clips/cogs_vs_clips_summarizer/**"
      - "reporters/reporter_sdk/**"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: metta-ai/reporters-cogs-vs-clips-summarizer

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute tags
        id: tags
        run: |
          REPO="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          SHORT_SHA="${GITHUB_SHA::12}"
          TAGS="${REPO}:latest,${REPO}:main-${SHORT_SHA}"
          echo "tags=${TAGS}" >> "$GITHUB_OUTPUT"
          echo "primary_tag=${REPO}:main-${SHORT_SHA}" >> "$GITHUB_OUTPUT"

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./reporters
          file: ./reporters/cogs_vs_clips/cogs_vs_clips_summarizer/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.tags.outputs.tags }}
