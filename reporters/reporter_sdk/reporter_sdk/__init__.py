"""Shared primitives and helpers for coworld reporter implementations.

This package is consumed by templates under ``reporters/templates/`` and by
concrete game-specific reporters under ``reporters/<coworld>/``. It exists so
the v1 reporter contract (see ``docs/REPORTER_DESIGN.md``) is encoded once,
not re-derived per reporter.

Status: scaffold only — no implementation yet.
"""
